# chrome-in-vscode

VS Code から Chrome を起動するサンプル拡張です。

## 設定項目
- `codingAgent.chromePath`: Chrome 実行ファイルへのパス
- `codingAgent.agentUrl`: エージェントの URL

`Open Chrome` コマンド実行時にこれらの値が空の場合、設定画面へのリンク付きエラーメッセージが表示されます。
